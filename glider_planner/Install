Glider Planner Installation notes.

Dependencies:

- ROS hydro (or later). This is a ROS Hydro package that must be included in a catkin workspace
- sparser
- functions
- simulator
- libboost-regex

Installation steps

1) Go to the root directory of the library
2) Create a build directory 
     mkdir build
3) Go to this directory
     cd build
4) Configure the project
     cmake ..
5) Build the project
     make